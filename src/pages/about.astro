---
import PageLayout from '../layouts/PageLayout.astro';
import { Content, frontmatter } from '../data/about.md';
---

<PageLayout
  title={frontmatter.title}
  cover={frontmatter.cover}
  subclass={frontmatter.subclass}
>
  <Content />
</PageLayout>
